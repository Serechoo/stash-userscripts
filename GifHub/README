# Pornhub GIF Scraper

This Python script allows you to scrape GIF links from Pornhub based on a given search term. The script generates an HTML gallery that you can view in your browser.

## Prerequisites

Make sure you have the following installed:

- Python 3
- BeautifulSoup4
- Requests

You can install the required dependencies using:

pip install beautifulsoup4 requests
